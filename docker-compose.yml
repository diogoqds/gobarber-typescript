version: "3"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: nodejs
    ports:
      - 3333:3333
    volumes:
      - .:/usr/app
    depends_on:
      - db
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: "postgres123"
